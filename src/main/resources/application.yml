spring:
  application:
    name: marathon-diary-api

server:
  port: 8080

app:
  cors:
    allowed-origins:
      - http://localhost:3000
      - http://127.0.0.1:3000

logging:
  level:
    root: INFO
    kmg.marathondiary.api: DEBUG
